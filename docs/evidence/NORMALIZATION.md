# Evidence Normalization

Normalization converts heterogeneous tool/system outputs into common structured representations.

Conceptual stages:

```
Raw Output
 → Parse
 → Validate
 → Normalize
 → Attach Provenance
 → Correlate
```

Normalization must preserve source meaning and must not silently invent fields or certainty.